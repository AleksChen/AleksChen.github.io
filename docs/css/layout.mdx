---
title: CSS 布局
---

import ReferenceList from "@site/src/components/ReferenceList";

# 垂直水平居中

- 水平居中

  - 对于 行内元素: text-align: center;

  - 对于确定宽度的块级元素：

  - width 和 margin 实现。margin: 0 auto;

  - 绝对定位和 margin-left: -width/2, 前提是父元素 position: relative

  - 对于宽度未知的块级元素

  - table 标签配合 margin 左右 auto 实现水平居中。使用 table 标签（或直接将块级元素设值为 display:table），再通过给该标签添加左右 margin 为 auto。

  - inline-block 实现水平居中方法。display：inline-block 和 text-align:center 实现水平居中。

  - 绝对定位+transform，translateX 可以移动本身元素的 50%。

  - flex 布局使用 justify-content:center

- 垂直居中

  - 利用 line-height 实现居中，这种方法适合纯文字类

  - 通过设置父容器 相对定位，子级设置 绝对定位，标签通过 margin 实现自适应居中

  - 弹性布局 flex:父级设置 display: flex; 子级设置 margin 为 auto 实现自适应居中

  - 父级设置相对定位，子级设置绝对定位，并且通过位移 transform 实现

  - table 布局，父级通过转换成表格形式，然后子级设置 vertical-align 实现。（需要注意的是：vertical-align: middle 使用的前提条件是内联元素以及 display 值为 table-cell 的元素）。

## 居中元素尺寸固定

### absoulte + 负 margin

```Css

.father {
    position: relative;
}
.son {
    position: absolute;;
    top: 50%;
    left: 50%;
    margin-left: -50px;
    margin-top: -50px;
}
```

### absoulte + calc

```Css

.father {
    position: relative;
}
.son {
    position: absolute;;
    top: calc(50% - 50px);
    left: calc(50% - 50px);
}
```

### table-cell

这种方式需要父元素的宽高都是确定的，才能保证子元素在父元素中垂直水平都居中。

```Css

.father {
    height: 500px;
    width: 500px;
    display: table-cell;
    vertical-align: middle;
text-align: center;
}
.son {
    display: inline-block;

}
```

## 居中元素尺寸不固定

### absoulte + margin auto

```Css

.father {
    position: relative;
}
.son {
    position: absolute;;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    margin: auto;
}
```

### absolute + transform

```css
.father {
  position: relative;
}
.son {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}
```

### flex

```html
<div class="father">
  <div class="son">123123</div>
</div>
```

```css
.father {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### grid（兼容性存在问题）

```html
<div class="father">
  <div class="son">123123</div>
</div>
```

```css
.father {
  display: grid;
}
.son {
  align-self: center; /*设置单元格内容的垂直位置*/
  justify-self: center; /*设置单元格内容的水平位置*/
}
```

## 行内元素垂直水平居中

### line-height

```html
<div class="father">
  <div class="son">123123</div>
</div>
```

```css
.father {
  width: 500px;
  height: 500px;
  line-height: 500px;
  text-align: center;
}
.son {
  display: inline-block;
  vertical-align: middle;
  line-height: inherit;
}
```

## 参考

<ReferenceList
  data={[
    {
      title: "CSS 实现水平垂直居中的 10 种方式",
      link: "https://juejin.cn/post/6844903679242305544",
    },
    {
      title: "CSS 垂直居中的正确打开方式",
      link: "https://juejin.cn/post/6991465721565806605",
    },
    {
      title: "这几种经典 CSS 布局问题你都能解决嘛？",
      link: "https://mp.weixin.qq.com/s/yqHKaRJnBlWkXAkCru7Pew",
    },
  ]}
/>
